import 'package:flutter/material.dart';

class Choice {
  const Choice( {this.title, this.keyword});
  final String title;
  final String keyword;
}

class PassData{
  String id;
  PassData({this.id});

}